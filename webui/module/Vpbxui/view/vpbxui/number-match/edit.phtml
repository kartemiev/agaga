<?php

$title = 'Редактирование фильтра номера';
$this->headTitle($title);
?>
<h3><?php echo $this->escapeHtml($title); ?></h3>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url(
    'vpbxui/settings/filter',
    array(
        'action' => 'edit',
        'id'     => $this->id,
    )
));

echo $this->partial('filterFormPartial.phtml',array('form' => $form));
