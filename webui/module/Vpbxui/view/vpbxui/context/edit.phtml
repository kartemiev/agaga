<?php

$title = 'Редактирование контекста';
$this->headTitle($title);
?>
<h3><?php echo $this->escapeHtml($title); ?></h3>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url(
    'vpbxui/settings/context',
    array(
        'action' => 'edit',
        'id'     => $this->id,
    )
));

echo $this->partial('contextFormPartial.phtml',array('form' => $form));
